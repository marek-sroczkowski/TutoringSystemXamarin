<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms" 
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:TutoringSystemMobile.Views"
       xmlns:viewModel="clr-namespace:TutoringSystemMobile.ViewModels"
       Title="TutoringSystemMobile"
       x:Class="TutoringSystemMobile.AppShell">

    <Shell.BindingContext>
        <viewModel:AppShellViewModel />
    </Shell.BindingContext>

    <!--
        The overall app visual hierarchy is defined here, along with navigation.
    
        https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/
    -->

    <Shell.Resources>
        <ResourceDictionary>
            <Style x:Key="BaseStyle" TargetType="Element">
                <Setter Property="Shell.BackgroundColor" Value="{AppThemeBinding Light=#2196F3, Dark=Black}" />
                <Setter Property="Shell.ForegroundColor" Value="White" />
                <Setter Property="Shell.TitleColor" Value="White" />
                <Setter Property="Shell.DisabledColor" Value="#B4FFFFFF" />
                <Setter Property="Shell.UnselectedColor" Value="#95FFFFFF" />
                <Setter Property="Shell.TabBarBackgroundColor" Value="{StaticResource Primary}" />
                <Setter Property="Shell.TabBarForegroundColor" Value="White"/>
                <Setter Property="Shell.TabBarUnselectedColor" Value="#95FFFFFF"/>
                <Setter Property="Shell.TabBarTitleColor" Value="White"/>
            </Style>
            <Style TargetType="TabBar" BasedOn="{StaticResource BaseStyle}" />
            <Style TargetType="FlyoutItem" BasedOn="{StaticResource BaseStyle}" />

            <!--
            Default Styles for all Flyout Items
            https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/flyout#flyoutitem-and-menuitem-style-classes
            -->
            <Style Class="FlyoutItemLabelStyle" TargetType="Label">
                <Setter Property="TextColor" Value="White"></Setter>
            </Style>
            <Style Class="FlyoutItemLayoutStyle" TargetType="Layout" ApplyToDerivedTypes="True">
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="{x:OnPlatform UWP=Transparent, iOS=White}" />
                                    <Setter TargetName="FlyoutItemLabel" Property="Label.TextColor" Value="{StaticResource Primary}" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Selected">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>

            <!--
            Custom Style you can apply to any Flyout Item
            -->
            <Style Class="MenuItemLayoutStyle" TargetType="Layout" ApplyToDerivedTypes="True">
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal">
                                <VisualState.Setters>
                                    <Setter TargetName="FlyoutItemLabel" Property="Label.TextColor" Value="{StaticResource Primary}" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Shell.Resources>

    <!-- 
        When the Flyout is visible this defines the content to display in the flyout.
        FlyoutDisplayOptions="AsMultipleItems" will create a separate flyout item for each child element    
        https://docs.microsoft.com/dotnet/api/xamarin.forms.shellgroupitem.flyoutdisplayoptions?view=xamarin-forms
    -->
    <FlyoutItem Title="Start" Icon="icon_home.png" IsVisible="{Binding IsTutor}">
        <ShellContent Route="StartTutorPage" ContentTemplate="{DataTemplate local:StartTutorPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Rezerwacje" Icon="icon_reservations.png" IsVisible="{Binding IsTutor}">
        <ShellContent Route="ReservationTutorPage" ContentTemplate="{DataTemplate local:ReservationTutorPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Dodatkowe zlecenia" Icon="icon_orders.png" IsVisible="{Binding IsTutor}">
        <ShellContent Route="OrdersTutorPage" ContentTemplate="{DataTemplate local:OrdersTutorPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Statystyki" Icon="icon_report.png" IsVisible="{Binding IsTutor}">
        <ShellContent Route="ReportTutorPage" ContentTemplate="{DataTemplate local:ReportTutorPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Studenci" Icon="icon_students.png" IsVisible="{Binding IsTutor}">
        <ShellContent Route="StudentsTutorPage" ContentTemplate="{DataTemplate local:StudentsTutorPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Przedmioty" Icon="icon_subjects.png" IsVisible="{Binding IsTutor}">
        <ShellContent Route="SubjectsTutorPage" ContentTemplate="{DataTemplate local:SubjectsTutorPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Konto" Icon="icon_account.png" IsVisible="{Binding IsTutor}">
        <ShellContent Route="MyProfilePage" ContentTemplate="{DataTemplate local:MyProfilePage}" />
    </FlyoutItem>

    <FlyoutItem Title="Start" Icon="icon_home.png" IsVisible="{Binding IsStudent}">
        <ShellContent Route="StartStudentPage" ContentTemplate="{DataTemplate local:StartStudentPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Rezerwacje" Icon="icon_reservations.png" IsVisible="{Binding IsStudent}">
        <ShellContent Route="ReservationStudentPage" ContentTemplate="{DataTemplate local:ReservationStudentPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Korepetytorzy" Icon="icon_tutors.png" IsVisible="{Binding IsStudent}">
        <ShellContent Route="TutorsStudentPage" ContentTemplate="{DataTemplate local:TutorsStudentPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Konto" Icon="icon_account.png" IsVisible="{Binding IsStudent}">
        <ShellContent Route="MyProfilePage" ContentTemplate="{DataTemplate local:MyProfilePage}" />
    </FlyoutItem>

    <!-- When the Flyout is visible this will be a menu item you can tie a click behavior to  -->
    <MenuItem Text="Wyloguj się" IconImageSource="icon_logout.png" StyleClass="MenuItemLayoutStyle" Clicked="OnMenuItemClicked">
    </MenuItem>

    <!--
        TabBar lets you define content that won't show up in a flyout menu. When this content is active
        the flyout menu won't be available. This is useful for creating areas of the application where 
        you don't want users to be able to navigate away from. If you would like to navigate to this 
        content you can do so by calling 
        await Shell.Current.GoToAsync("//LoginPage");
    -->
    <TabBar>
        <ShellContent Route="LoginPage" ContentTemplate="{DataTemplate local:LoginPage}"/>
    </TabBar>

    <TabBar>
        <ShellContent Route="AccountActivationPage" ContentTemplate="{DataTemplate local:AccountActivationPage}" />
    </TabBar>


    <!-- Optional Templates 
    // These may be provided inline as below or as separate classes.

    // This header appears at the top of the Flyout.
    // https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/flyout#flyout-header
    <Shell.FlyoutHeaderTemplate>
        <DataTemplate>
            <Grid>ContentHere</Grid>
        </DataTemplate>
    </Shell.FlyoutHeaderTemplate>

    // ItemTemplate is for ShellItems as displayed in a Flyout
    // https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/flyout#define-flyoutitem-appearance
    <Shell.ItemTemplate>
        <DataTemplate>
            <ContentView>
                Bindable Properties: Title, Icon
            </ContentView>
        </DataTemplate>
    </Shell.ItemTemplate>

    // MenuItemTemplate is for MenuItems as displayed in a Flyout
    // https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/flyout#define-menuitem-appearance
    <Shell.MenuItemTemplate>
        <DataTemplate>
            <ContentView>
                Bindable Properties: Text, Icon
            </ContentView>
        </DataTemplate>
    </Shell.MenuItemTemplate>

    -->

</Shell>
